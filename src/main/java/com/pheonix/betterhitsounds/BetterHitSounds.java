package com.pheonix.betterhitsounds;

import net.fabricmc.api.ModInitializer;

public class BetterHitSounds implements ModInitializer {
    @Override
    public void onInitialize() {
        // Intentionally empty (client-only mod)
    }
}
